[pytest]
# Opciones por defecto: modo verbose, trazas de error cortas y registro de marcas
addopts = -v --tb=short --strict-markers

# Dónde buscar los tests
testpaths = tests

# Marcadores personalizados para evitar warnings
markers =
    unit: tests unitarios rápidos
    integration: tests de integración